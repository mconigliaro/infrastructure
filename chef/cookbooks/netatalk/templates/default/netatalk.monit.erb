check process afpd with pidfile /var/run/afpd.pid
  start program = "/usr/sbin/service netatalk start"
  stop program  = "/usr/sbin/service netatalk stop"
  if 3 restarts within 5 cycles then timeout

check process cnid_metad with pidfile /var/run/cnid_metad.pid
  start program = "/usr/sbin/service netatalk start"
  stop program  = "/usr/sbin/service netatalk stop"
  if 3 restarts within 5 cycles then timeout
