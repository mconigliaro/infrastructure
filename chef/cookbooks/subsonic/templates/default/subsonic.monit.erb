check process subsonic with pidfile /var/subsonic/subsonic.pid
  start program = "/usr/sbin/service subsonic start"
  stop program  = "/usr/sbin/service subsonic stop"
  if failed port 4040 type TCPSSL with timeout 15 seconds for 3 cycles then restart
  if 3 restarts within 5 cycles then timeout
