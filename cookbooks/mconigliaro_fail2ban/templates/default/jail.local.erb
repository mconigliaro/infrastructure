# FIXME: Ubuntu 16.04 contains incorrect logpath defaults for many services
# See: https://github.com/fail2ban/fail2ban/issues/1396
<% if @dovecot_enabled %>
[dovecot]
enabled = true
logpath = /var/log/mail.log
<% end %>
<% if @postfix_enabled %>
[postfix]
enabled = true
logpath = /var/log/mail.log
<% end %>
