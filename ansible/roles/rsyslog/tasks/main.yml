---
- name: install rsyslog
  apt:
    name: rsyslog

- name: start rsyslog
  service:
    name: rsyslog
    state: started

- name: configure monit for rsyslog
  template:
    src: rsyslog.monit.j2
    dest: /etc/monit/conf.d/rsyslog
  notify:
    - restart monit
